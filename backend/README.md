# README

Instalar dependencias

- cd backend
- bundle install

- Edita config/database.yml si es necesario, luego:
- rails db:create
- rails db:migrate
- rails db:seed # opcional, si hay datos de prueba

rails s
